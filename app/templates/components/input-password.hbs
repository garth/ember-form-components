{{input-text type="password" label="Password" placeholder="password"
  value=view.value formController=view required=view.required
  customValidator=view.validatePassword}}
{{partial input_password_strength}}
{{input-text type="password" label="Confirm Password" placeholder="password"
  formController=view
  shouldEqual=view.value shouldEqualMessage="Passwords do not match"}}
